# AXIOMTEK 報價單產生器

艾訊 (AXIOMTEK) 業務用的網頁版報價單產生器。在瀏覽器中直接填寫、計算並列印 / 另存 PDF 的報價單；報價單歷史與常用料號清單皆可於登入後跨裝置雲端同步。

程式由三個檔案組成：`index.html`（結構）、`styles.css`（樣式）、`app.js`（邏輯），無需建置流程，可直接於瀏覽器開啟。

## 功能

- **報價單編輯**：品牌表頭、客戶 / 業務資料、注意事項、幣別、備註、簽署欄，多數欄位可直接於頁面上編輯。
- **品項表格**：動態增減品項列，自動計算單項總價、合計、稅額與總計。
- **可調稅率**：稅率預設 5%，可直接編輯，支援 0% 免稅 (外銷) 情境。
- **幣別連動**：金額格式跟隨幣別欄位，變更幣別時所有金額即時更新。
- **成本 / 毛利分析**：每列顯示成本與毛利 (GP%、每件毛利金額，負毛利以紅色標示)，底部彙總成本合計、毛利與毛利率。
  - 成本 / 毛利欄位**僅在螢幕上顯示，列印 / 另存 PDF 時會自動隱藏**，避免內部成本外流。
- **自動暫存**：目前編輯中的報價單會自動存入瀏覽器本機，重新整理或關閉後再開啟會自動還原。
- **報價單存檔與歷史紀錄**：可將報價單命名存檔，於「已存報價單」面板中開啟、複製或刪除；登入後即時同步至雲端，跨裝置共用。
  - **自動編號**：報價單號由系統自動產生，格式為 `AXTW`+年月日(6碼)+當日流水號(2碼)+`-v.`版次(2碼)，例如 `AXTW26072001-v.01`。
  - **修正版次**：對已存報價單按「修正」會沿用同一單號、版次+1，並可填寫「變更原因」；同系列的所有版次會收合顯示，可展開查看歷史版本與異動原因。
  - **依客戶名稱搜尋**：已存報價單清單上方可輸入客戶名稱關鍵字即時篩選。
- **客戶 / 業務資料記憶**：客戶名稱與聯絡窗口、業務姓名與聯絡方式會自動記錄；再次輸入相同名稱時自動帶出聯絡電話與 Email，登入後跨裝置同步。
- **常用料號清單**：報價時輸入完整料號 / 品名 / 規格會自動記錄；再次輸入同一料號可自動帶出品名、規格、單價與成本，反過來輸入規格敘述也能帶出料號與品名。
- **匯入 / 匯出**：
  - 匯入既有料號資料 (Excel `.xlsx` / `.xls` / CSV)，含欄位自動對應與確認介面。
  - 常用料號清單可匯出 / 匯入 JSON 備份。
- **雲端同步**：透過 Firebase 帳號登入後，常用料號清單與報價單歷史即時同步至 Firestore，跨裝置共用；未登入時退回瀏覽器本機儲存 (localStorage)。
- **列印 / PDF**：透過瀏覽器列印功能輸出，並針對列印調整版面。
- **響應式版面**：螢幕寬度 760px 以下自動切換為單欄排版。

## 使用方式

線上版本：<https://axiomtektw.github.io/quote/>

亦可直接以瀏覽器開啟 `index.html` 於本機使用。

雲端同步與 Excel 匯入功能需要網路連線 (相依套件透過 CDN 載入)。

## 技術

- 純靜態網頁 (`index.html` + `styles.css` + `app.js`)，無需建置流程。
- [SheetJS (xlsx)](https://sheetjs.com/) — Excel / CSV 讀取。
- [Firebase](https://firebase.google.com/) (Auth + Firestore) — 帳號登入與雲端同步。
- 資料儲存：未登入使用 `localStorage`；登入後使用 Firestore。
- Firestore 安全性規則見 [`firestore.rules`](firestore.rules)，每位使用者僅能存取自己 uid 底下的資料。

## 開發

自動快取破除：`.githooks/pre-commit` 會在每次 commit 時，以 `app.js` / `styles.css` 的內容雜湊標記 `index.html` 的引用 (例如 `app.js?v=1a2b3c4d`)，內容一變雜湊即變，瀏覽器便會重新下載，避免使用者看到舊版快取。

**每個 clone 首次啟用一次**：

```
git config core.hooksPath .githooks
```

## 待辦 / 已知事項

- 報價單雲端同步需先於 Firebase 套用 [`firestore.rules`](firestore.rules)，使規則涵蓋 `users/{uid}/quotes`。
- 目前所有報價單存於單一 Firestore 文件，資料量極大時可考慮改為每張報價單一份文件。
